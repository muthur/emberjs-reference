{{credit-card
  cardNumber=cardNumber
  expiryDate=expiryDate
  cvv=cvv
  nameOnCard=nameOnCard
  zipCode=zipCode
  selectedCountry=userCountry
  errors=errors
  onValidityChange=(action "onCreditCardValidityChange")
}}

{{cardNumber}}
creditCardInfoIsValid{{creditCardInfoIsValid}}

{{#paper-button raised=true primary=true disabled=(if (not creditCardInfoIsValid) true null) }}Add Payment{{/paper-button}}